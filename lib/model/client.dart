
class Client {
  String person_name;
  String category;
  double rating;
  int distance;
  int max_price;
  String currency;
  int fee;

  Client(this.person_name, this.category, this.rating, this.distance,
      this.max_price, this.currency, this.fee);
}
